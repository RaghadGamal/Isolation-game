def print_board(board, posA, posB):
    # الدالة دي بتطبع الـ board بتاع اللعبة على الشاشة بشكل منسق
    # board: مصفوفة ثنائية الأبعاد (2D list) بتمثل كل خانات اللعبة
    # posA: tuple (row, column) تمثل مكان اللاعب A
    # posB: tuple (row, column) تمثل مكان اللاعب B

    print("\nBoard:")  # طباعة عنوان قبل ما نبدأ نعرض اللوحة، \n يعني سطر جديد عشان يبقى فيه مسافة من الطباعة اللي قبلها

    for r in range(SIZE):  # لوب بيمر على كل الصفوف في الـ board
        row_str = ""  # متغير مؤقت لتخزين النص اللي هيتم طباعته للصف ده
        for c in range(SIZE):  # لوب جوا اللي بيعدي على كل الأعمدة في الصف r
            if (r, c) == posA:  # لو الخانة الحالية هي مكان اللاعب A
                row_str += "A "  # حط حرف A
            elif (r, c) == posB:  # لو الخانة الحالية هي مكان اللاعب B
                row_str += "B "  # حط حرف B
            elif board[r][c] == BLOCK:  # لو الخانة دي عبارة عن عائق أو بلوك
                row_str += "X "  # حط حرف X للدلالة على البلوك
            else:  # لو مفيش حاجة خاصة بالخانة دي
                row_str += ". "  # حط نقطة للدلالة على خانة فاضية
        print(row_str)  # بعد ما نخلص كل الأعمدة في الصف، اطبع الصف كله
    print()  # سطر فاضي بعد طباعة اللوحة عشان يبقى شكلها مرتب